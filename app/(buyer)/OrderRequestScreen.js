import { View, Text } from 'react-native';

export default function OrderRequestScreen() {
  return (
    <View className="flex-1 bg-light items-center justify-center">
      <Text className="text-dark font-bold text-lg">
        Order Request — coming soon
      </Text>
    </View>
  );
}